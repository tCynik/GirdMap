package com.example.lessongirdrecycler.domain.models.global

/**
 * в данном случае глобальные координаты ИНТОВЫЕ
 * При использовании структуры с GPS координатами необходимо переписать систему координат в ДАБЛ
 * при этом учесть количество знаков после запятой (исходя из кратности масштаба)
 */
class GlobalCoordinatesMapper {
    fun mapGlobalToLocal (globalTurnPoint: GlobalTurnPoint):
}